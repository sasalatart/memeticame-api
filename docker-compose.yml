postgres:
  image: postgres:9.5.0

web:
  build: .
  command: puma -b tcp://0.0.0.0:3000
  ports:
    - '80:3000'
    - '443:3000'
  links:
    - postgres
  environment:
    - SECRET_KEY_BASE
    - FCM_KEY
